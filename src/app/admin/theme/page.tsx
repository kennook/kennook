import { PlaceholderSection } from '@/components/admin/PlaceholderSection';

export default function AdminThemePage() {
  return (
    <PlaceholderSection
      title="Theme"
      description="Customise the look of this KenNook instance — accent colors, logo, branding, dark/light scheme. Today the app ships a fixed dark theme; this section will expose tokens that other components can read."
    />
  );
}
