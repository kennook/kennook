import { PlaceholderSection } from '@/components/admin/PlaceholderSection';

export default function AdminSettingsPage() {
  return (
    <PlaceholderSection
      title="Settings"
      description="Instance-wide configuration — index roots, sensitive thresholds, default fit/zoom, slideshow defaults, and similar knobs that today live as constants or per-user preferences."
    />
  );
}
