import { PlaceholderSection } from '@/components/admin/PlaceholderSection';

export default function AdminUsersPage() {
  return (
    <PlaceholderSection
      title="Users"
      description="Manage the user list (Viewer, Admin, and any future roles). Create, rename, demote, delete. Today there are exactly two users seeded by the user.db v5 migration; this page will let an admin grow that list."
    />
  );
}
