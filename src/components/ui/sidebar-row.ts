/**
 * Shared className for a full-width row in the collapsible sidebars (the right
 * "utilities" aside, and reused wherever a sidebar list-item look is wanted).
 * Matches the left facets sidebar's item styling (see PlaylistsSection).
 */
export const SIDEBAR_ROW =
  'flex w-full items-center gap-2.5 rounded px-3 py-1.5 text-sm text-left ' +
  'text-zinc-400 hover:text-zinc-100 hover:bg-zinc-900/60 transition';
